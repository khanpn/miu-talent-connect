import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#0d3c7a',
    },
    secondary: {
      main: '#D4F6FF',
      dark: '#0CCFC1',
    },
  },
  shape: {
    borderRadius: 4,
  },
});
